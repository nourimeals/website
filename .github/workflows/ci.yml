name: Continuous Integration

on:
  pull_request:
    branches:
    - master

jobs:

  build:
    name: Run build
    
  runs-on: ubuntu-latest

  steps:
  - name: Checkout Code
    uses: actions/checkout@v1

  - name: Install Dependencies
    run: npm i